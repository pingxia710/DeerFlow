---
title: Lead Agent
description: Lead Agent 负责会话连续性和最终判断；在指挥室中，它保持脑袋清醒，由一次性 Codex 子 AI 执行、核对和反向审查。
---

import { Callout, Steps } from "nextra/components";

# Lead Agent

<Callout type="info">
  Lead Agent 保留用户目标、方案、进度、上下文、边界和最终判断。
  `command-room` 会把执行交给子 AI，让这个脑袋能持续指挥大型项目。
</Callout>

## 职责

Lead Agent 接收用户消息、维护会话状态、判断需要什么工作、编写完整交接
prompt、阅读各 AI 返回的自然结果，并给出最终回复。LangGraph/LangChain
负责状态、流式传输、检查点、中间件和工具调用，但这些机制不代替 AI 判断。

默认 Agent 和普通自定义 Agent 可以使用配置的直接工具；`command-room`
有意采用不同边界：

- 不获得文件、Shell、网页、MCP、图像检查或 skill 管理等直接执行工具；
- 即使客户端发送 `subagent_enabled: false`，`task()` 也始终启用；
- 直接工具只保留委派、必要的用户澄清和呈现已经完成的文件；
- 每个 worker 结果必须交给另一个核对 AI，并取得独立反方结果后再裁决。

专业角色是开发者写入 prompt 的上下文，不授予 DeerFlow 工具。每个任务只
启动一个临时 Codex CLI AI，由它自主规划和选择原生工具，返回完整自然语言
结果后结束。

## 指挥室流程

<Steps>

### 接收并定位

在主会话中保持用户目标、确认过的上下文、当前边界和进度。

### 委派执行

给一次性子 AI 写全专业角色、目标、相关事实和路径、权限、禁止事项、完成
标准及返回要求。独立工作可以并行，一条 lead 模型响应最多六个 `task()`。

### 核对与反方

把每个 worker 的完整结果交给不同的核对 AI；再让独立 opposition AI 从另
一个方向寻找遗漏假设、相反证据和风险。

### 裁决

阅读 worker、核对 AI 和反方的自然语言结果，判断目标是否完成、是否需要
另一个有边界的任务，或者是否必须让用户授权边界变化。程序元数据不做裁决。

### 持久化并回复

配置检查点后，LangGraph 保存消息和客观运行状态。向用户返回自然结论，并
在需要时呈现已经完成的产出物。

</Steps>

## 模型选择

Lead Agent 模型按以下顺序解析：

1. 有效的请求级 `model_name` / `model`；
2. 当前自定义 Agent 配置的模型；
3. `config.yaml` 中第一个模型。

子任务模型独立配置，不继承 Lead Agent：

```yaml
subagents:
  model: gpt-5.6-terra
  reasoning_effort: xhigh
  timeout_seconds: 3600
```

## 思考与计划模式

`thinking_enabled` 控制 Lead Agent 支持的推理能力。普通 Agent 可以通过
`is_plan_mode` 启用 `TodoMiddleware`。指挥室不把程序 todo list 当作工作流；
进度由主 AI 保持，执行和审查通过 prompt 委派。

## 自定义 Agent 与 Bootstrap

普通自定义 Agent 可以配置名称、指令、skills、直接工具组和默认 lead 模型。
它与 `subagents.custom_agents` 不同，后者只定义一次性专业角色的 prompt 上下文
和可选 Codex 模型。

Bootstrap 是创建普通自定义 Agent 时使用的最小设置模式，不是指挥室任务运行时。

<Cards num={2}>
  <Cards.Card title="子 Agent" href="/docs/harness/subagents" />
  <Cards.Card title="工具" href="/docs/harness/tools" />
</Cards>
